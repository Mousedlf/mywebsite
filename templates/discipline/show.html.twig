{% extends 'base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discipline.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ discipline.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discipline_index') }}" class="btn">back to list</a>

    <a href="{{ path('app_discipline_edit', {'id': discipline.id}) }}" class="btn btn-warning">edit</a>

    {{ include('discipline/_delete_form.html.twig') }}
{% endblock %}
