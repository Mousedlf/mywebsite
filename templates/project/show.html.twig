{% extends 'base.html.twig' %}

{% block title %}Hello ProjectController!{% endblock %}

{% block body %}

    <div class="col-9">
        <h1 class="mt-5">{{ project.name }}</h1>
        <p>{{ project.description }}</p>

    </div>

    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('edit_project', {id:project.id}) }}" class="badge rounded-pill text-bg-warning">edit</a>
    <a href="{{ path('delete_project', {id:project.id}) }}" class="badge rounded-pill text-bg-danger">remove</a>
    <a href="{{ path('add_image', {id:project.id}) }}" class="badge rounded-pill text-bg-warning">manage images</a>
    {% endif %}

    <p class="card mt-5 text-center">images ici</p>
{#
    {% for image in project.images %}
        <div class="card">
            <img src="{{ vich_uploader_asset(image, 'imageFile') }}" alt="">
        </div>
    {% endfor %}#}






{% endblock %}
