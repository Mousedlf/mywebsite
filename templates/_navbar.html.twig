
    <nav class="navbar navbar-expand-lg mx-5">
        <div class="container-fluid d-flex justify-content-between mt-2">
            <div class="">
                <a class="navbar-brand" href="{{ path('app_home') }}">
                    <img src="{{ asset('placeholder.svg') }}" alt="my little mouse logo" height="60px">
                    <a href="" class="btn text-uppercase kalam">about me</a>
                    <span>|</span>
                    <a href="{{ path('app_project') }}" class="btn text-uppercase kalam">projects</a>
                </a>
            </div>


            {% if is_granted('ROLE_ADMIN') %}
            <a class="nav-link active" aria-current="page" href="{{path('new_project') }}">New Project</a>
            {% endif %}


        </div>
    </nav>
